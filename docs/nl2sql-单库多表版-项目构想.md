# 企业级 NL2SQL 平台项目构想（单库多表 MVP 版）

## 1. 项目定位与背景

基于我们对"多数据库、多表联合查询"终极愿景的讨论，我们意识到跨库联邦查询（涉及异构数据源和极高的语义冲突）在初期落地时风险极高。因此，本篇文档聚焦于项目的 **MVP 落地阶段：多数据源接入，限定单库多表查询**。

在这一阶段，系统的使用模式是：

1. 系统后台配置了多个数据源（例如 MySQL 订单库、PostgreSQL 用户库、Oracle 财务库）。
2. 员工在使用时，**必须通过界面下拉框明确选择"当前要查询的数据库/业务域"**。
3. 用户输入大白话，大模型仅在**当前选定的单库范围内**，进行多表联合查询，生成 SQL，并返回数据或图表。

> **措辞说明**：本文档标题为"单库多表"，指的是**查询范围限定在单库内**，而非系统只支持一个数据库。系统支持接入多个数据源，但同一时刻用户的查询不会跨库。

这种模式极大地降低了大模型的认知负担，同时完全保留了项目最核心的创新点（`skills.md`、字段 tag、可信度体系、数据治理）。

---

## 2. 核心创新点在"单库多表"下的应用

### 2.1 动态检索 Schema RAG (解决单库表数量过多的问题)

在一个企业级单库中，往往存在几百上千张表。如果把所有表结构硬塞给大模型，由于上下文窗口限制以及注意力稀释，模型很容易产生幻觉。我们采用 **Schema RAG (检索增强生成)** 架构：

- **向量化**：系统将选定库中所有表的表名、字段、注释以及 `skills.md` 描述向量化，存入向量数据库。
- **动态召回**：当用户提问时，系统先进行相似度检索，**精准召回与当前问题最相关的 Top 5~10 张表**。
- **专注生成**：只把这 5~10 张表作为 Context 喂给大模型生成单库 SQL，既支持了庞大的数据库，又保证了 SQL 生成的高准确率。

### 2.2 引入业务外挂知识：`skills.md`

单靠 schema 无法应对复杂的业务提问。在单库场景下，我们依然需要为该库配置 `skills.md`（业务语义说明书）。

在这个文档中记录：

- 该库的核心主流程
- 事实表与维度表的明确区分与关联关系（Join 规则）
- 关键字段的真实业务含义和统计口径
- 哪些字段可用，哪些是废弃/冗余字段
- **常见复杂查询的 Few-shot SQL 模板**（如窗口函数、多层嵌套子查询等）

### 2.3 字段 Tag 与可信度自学习体系

由于很多字段没有注释或注释不准，系统需要给每个字段建立动态档案：

- **Tag 打标**：标记字段是"金额类""时间类""枚举状态类"等。
- **可信度流转**：（详见第 7 节可信度量化算法）

这让系统不再是静态的查数工具，而是具备"人机协同学习"能力的活系统。

### 2.4 轻量级数据治理与血缘闭环（单库依然必不可少）

即便在单库范围内，表结构的混乱（如同名字段不同口径、废弃字段遗留）也会导致大模型频频翻车。因此，必须将数据治理与血缘追踪融入系统的核心流程中：

- **基于血缘的查询自愈**：通过大模型生成的 SQL 记录，自动逆向解析并沉淀出"单库表级/字段级血缘"。当数据库某张表的字段发生类型变更或被删除时，系统通过血缘图谱立刻告警，并提示修改对应的 `skills.md`，防止下一次问答报错。
- **治理探针与废弃隔离**：在 Tag 体系中设立"废弃/敏感/不推荐"标签。对于历史遗留的脏表和脏字段，通过治理工作台将其隔离，在 RAG 召回时直接屏蔽，避免污染大模型的上下文。

---

## 3. 系统整体架构设计

### 3.1 分层架构概览

```
┌─────────────────────────────────────────────────────────────────────┐
│                         前端（Vue/React）                            │
│         员工问答端（C端）          后台治理管理端（B端）               │
└──────────────┬──────────────────────────────────┬───────────────────┘
               │          统一 HTTP API            │
               ▼                                    ▼
┌──────────────────────────────────────────────────────────────────────┐
│                  Java 网关层（Spring Boot）                           │
│  ┌──────────┐ ┌──────────┐ ┌───────────┐ ┌──────────┐ ┌──────────┐ │
│  │ 用户鉴权  │ │ 数据源   │ │ skills.md │ │ 可信度   │ │ 查询审计 │ │
│  │ 与权限    │ │ 管理     │ │ 管理      │ │ 管理     │ │ 日志     │ │
│  └──────────┘ └──────────┘ └───────────┘ └──────────┘ └──────────┘ │
│              ┌─────────────────────────────────────┐                 │
│              │   内部 HTTP 调用（OkHttp/Feign）     │                 │
│              └──────────────┬──────────────────────┘                 │
└─────────────────────────────┼────────────────────────────────────────┘
                              ▼
┌─────────────────────────────────────────────────────────────────────┐
│                 Python AI 服务（FastAPI + LangGraph）                │
│  ┌──────────────┐ ┌──────────────┐ ┌──────────────┐ ┌────────────┐ │
│  │ Schema RAG   │ │ SQL 生成     │ │ SQL 执行     │ │ 图表生成   │ │
│  │ 向量召回     │ │ (LLM)       │ │ (沙箱)       │ │ (LLM)     │ │
│  └──────────────┘ └──────────────┘ └──────────────┘ └────────────┘ │
│              ┌─────────────────────────────────────┐                 │
│              │        LangGraph 有向图编排          │                 │
│              └─────────────────────────────────────┘                 │
└─────────────────────────────┬────────────────────────────────────────┘
                              │
        ┌─────────────────────┼─────────────────────┐
        ▼                     ▼                     ▼
┌──────────────┐   ┌──────────────┐   ┌──────────────────┐
│  向量数据库   │   │  业务数据库   │   │  大模型 API      │
│ (Milvus/     │   │ (MySQL/PG/   │   │ (GPT/Claude/    │
│  Chroma)     │   │  Oracle)     │   │  私有部署)       │
└──────────────┘   └──────────────┘   └──────────────────┘
```

### 3.2 服务通信模型（关键设计决策）

**决策：前端统一调 Java，Java 内部代理调 Python。**

```
前端 ──HTTP──▶ Java (网关) ──内部HTTP──▶ Python (AI服务)
                │                           │
                ├─ 用户鉴权                  ├─ 向量检索
                ├─ 权限校验                  ├─ SQL 生成
                ├─ 数据源路由                ├─ SQL 执行（直连数据库）
                ├─ 可信度查询                ├─ 图表生成
                └─ 审计日志记录              └─ 结果返回
```

**为什么这样设计：**

| 方案 | 优点 | 缺点 |
|------|------|------|
| A. 前端分别调 Java 和 Python | Python 可独立扩展 | 前端需管理两套认证、两个服务地址；CORS/Session 共享复杂 |
| B. 前端调 Java，Java 代理调 Python | 统一入口、统一鉴权、前端简单 | Java 多一跳转发，但内网延迟可忽略 |
| C. 前端调 Python，Python 调 Java 取权限 | AI 链路短 | Python 需要实现鉴权逻辑，职责不单一 |

**选择方案 B**，理由：前后端分离项目中，统一网关是标准做法，且 Java 天然适合做鉴权和管理类 CRUD。

**Java 调 Python 的具体方式：**

- **协议**：内部 HTTP（Spring Boot 用 `RestTemplate` 或 `OpenFeign` 调 Python 的 FastAPI）
- **调用时机**：仅在"问答查询"场景下 Java 才调 Python；后台管理功能（数据源配置、skills.md 编辑、可信度调整等）纯 Java 完成
- **超时设置**：Java 端设置 60 秒超时（含大模型推理 + SQL 重试），超时后返回友好提示
- **Python 无状态**：Python AI 服务不持有用户 Session，每次请求由 Java 传入必要的上下文（用户 ID、选中的数据源 ID、权限范围等）

**请求完整流转示例（用户提问 "华东区上月退款金额"）：**

```
1. 前端 POST /api/query/ask
   Body: { "question": "华东区上月退款金额", "datasourceId": 12, "conversationId": "abc" }

2. Java 网关层：
   a. 鉴权：校验 JWT Token，获取当前用户信息
   b. 权限：检查该用户是否有 datasourceId=12 的访问权限
   c. 获取数据源连接信息（JDBC URL，只读账号）
   d. 查询可信度信息（哪些字段是高可信，哪些是废弃）
   e. 调用审计日志：记录"谁在什么时间问了什么"
   f. 内部 HTTP 调 Python AI 服务，传入：
      {
        "question": "华东区上月退款金额",
        "datasourceId": 12,
        "jdbcUrl": "jdbc:mysql://...",
        "dbUsername": "readonly_user",
        "dbPassword": "encrypted:xxx",
        "highConfidenceFields": ["pay_amount", "refund_amount", ...],
        "deprecatedFields": ["old_user_id", ...],
        "conversationHistory": [
          {"role": "user", "content": "上个月订单总额多少"},
          {"role": "assistant", "content": "上月订单总额为 1,234,567 元"}
        ]
      }

3. Python AI 服务（LangGraph 有向图）：
   a. Schema_Retriever_Node → 向量库召回相关表
   b. SQL_Generator_Node → LLM 生成 SQL
   c. SQL_Validator_Node → AST 安全校验
   d. SQL_Executor_Node → 直连数据库执行
   e. (若报错 → 回到 b 重试，最多 3 次)
   f. Data_Visualizer_Node → LLM 生成 Echarts 配置
   g. 返回: { "sql": "...", "data": [...], "chartOption": {...}, "usedTables": [...], "usedFields": [...] }

4. Java 收到结果后：
   a. 更新字段可信度正向反馈（被成功使用的字段 +1）
   b. 记录 SQL 到审计日志（含血缘信息）
   c. 返回给前端

5. 前端展示表格 + 图表 + 口径说明
```

### 3.3 数据接入与预处理层

- 注册多个异构数据源，提取单库的 Schema。
- 将单库 Schema 与对应的 `skills.md` 解析后，写入向量数据库（如 Milvus / Chroma）。
- **数据源连接安全要求**（详见第 10 节）。

### 3.4 语义理解与召回层 (Schema RAG)

- 识别用户意图中的时间、维度、指标、筛选条件。
- 在当前选定的**单一数据库**内，召回相关的表和字段（Top 5-10）。
- **召回策略采用 Hybrid Search（混合检索）**（详见第 4 节）。

### 3.5 AI Agent 层（基于 Python / LangGraph 编排）

抛弃传统的 Java 线性调用，我们引入 **LangGraph** 构建带状态（Stateful）和自我纠错循环（Cyclic）的智能体工作流。
整个 NL2SQL 过程被定义为一个有向图（Graph），包含：

- **状态 (State)**：维护用户的原始问题、当前选中的数据库、召回的 Schema、生成的 SQL、执行报错信息、重试次数以及**历史对话摘要**。
- **节点 (Nodes)**：
  1. `Schema_Retriever_Node`：去向量库召回相关的表结构和 `skills.md`。
  2. `SQL_Generator_Node`：大模型根据 Schema 写出 SQL。
  3. `SQL_Validator_Node`：基于 AST 解析，进行安全校验（详见第 5 节）。
  4. `SQL_Executor_Node`：在沙箱中执行 SQL。如果报错，提取 Error Message。
  5. `Data_Visualizer_Node`：大模型根据执行成功的数据，决定画什么图（Echarts Option）。
- **条件边 (Conditional Edges)**：
  - `SQL_Validator_Node` 校验后，如果**不通过**（非 SELECT / 含危险函数）→ 直接返回安全告警给用户，不执行。
  - `SQL_Executor_Node` 执行后，如果**成功** → 流转到 `Data_Visualizer_Node`。
  - 如果**失败且重试次数 < 3** → 携带 Error Message 流转回 `SQL_Generator_Node` 让大模型自我修复（Self-Correction）。
  - 如果**失败且重试次数 >= 3** → 抛出异常给用户。

### 3.6 交互与反馈层 (闭环)

- 展示数据表格或自动推荐 Echarts 图表。
- **可解释性展示**：明确告诉用户"本次查询使用了 `order_info` 和 `user` 表，依赖了高可信字段 `pay_amount`"。
- **反馈收集**：用户点赞/踩，驱动底层字段可信度的升降级。
- **多轮对话支持**（详见第 6 节）。

---

## 4. Schema RAG 召回策略详解

### 4.1 为什么单纯向量检索不够

用户问"华东区退款率"，如果 RAG 阶段没有召回 `refund_record`（退款表），而是召回了 `order_info`（订单表），大模型生成的 SQL 必然是错的。在 RAG 链路中，**召回错了，生成一定错**。

### 4.2 Hybrid Search（混合检索）策略

系统采用三层召回融合，而非单纯依赖向量余弦相似度：

| 召回层 | 方法 | 作用 |
|--------|------|------|
| **语义召回** | 向量检索（Embedding + Cosine Similarity） | 理解自然语言与表/字段注释的语义关联 |
| **关键词召回** | BM25 全文检索 | 精确匹配字段名、表名、枚举值等关键词 |
| **模板召回** | 历史高频 SQL 模板匹配 | 当用户意图命中"排名前几""连续N天"等模式时，直接推荐已有 SQL 模板中的表 |

**融合方式**：三路召回的结果按加权分数排序，取 Top 5-10。权重初期可配置（如语义 0.5、关键词 0.3、模板 0.2），后续根据用户反馈自动调优。

### 4.3 引入阶段

- **阶段一（MVP）**：仅用向量检索，因为表数量少，先跑通链路。
- **阶段二**：引入 BM25 + 向量的 Hybrid Search，支撑 200+ 张表。
- **阶段三**：引入模板召回，结合历史高频查询优化召回质量。

---

## 5. SQL 执行安全与沙箱约束

### 5.1 SQL 安全拦截网关（SQL Parser & Validator）

在 AI 生成 SQL 后、数据库执行前，必须通过安全拦截网关：

| 校验项 | 规则 | 处理方式 |
|--------|------|----------|
| **语句类型** | 仅允许 `SELECT` | AST 解析，拦截 INSERT/UPDATE/DELETE/DROP/ALTER/TRUNCATE 等 |
| **危险函数** | 禁止 `SLEEP()`、`BENCHMARK()`、`LOAD_FILE()`、`INTO OUTFILE` 等 | AST 解析，黑名单匹配 |
| **子查询嵌套深度** | 最大 3 层 | 防止生成过于复杂导致数据库性能问题 |
| **表访问白名单** | 仅允许当前数据源中已启用的表 | 与后台配置的表启用列表比对 |
| **敏感字段过滤** | 禁止查询被标记为"敏感"的字段（如身份证号、工资） | 与字段 Tag 的"敏感"标签比对 |

**AST 解析工具选型**：
- MySQL：`jsqlparser`（Java）或 `sqlglot`（Python，推荐，支持多数据库方言）
- PostgreSQL：`sqlglot`
- Oracle：`sqlglot` 或 `calcite`

> **建议统一使用 Python 的 `sqlglot`**，因为 Python AI 服务负责 SQL 生成和执行，SQL 校验也在同一层完成，避免跨语言传递 AST。

### 5.2 SQL 执行沙箱约束

| 约束项 | 配置 | 说明 |
|--------|------|------|
| **只读事务** | 强制 `SET TRANSACTION READ ONLY` 或连接账号只有 SELECT 权限 | 从根源杜绝写操作 |
| **查询超时** | 单条 SQL 最大执行时间 30 秒 | 防止笛卡尔积等慢查询拖垮数据库 |
| **结果集限制** | 强制注入 `LIMIT 10000`（若用户未指定 LIMIT） | 防止全表扫描返回百万行 |
| **连接池隔离** | AI 服务使用独立的连接池，最大 10 连接 | 防止 AI 查询占用业务数据库所有连接 |
| **执行审计** | 每条执行的 SQL 都记录到审计日志 | 包含执行时间、影响行数、是否报错 |

### 5.3 SQL 拦截与沙箱的执行流程

```
LLM 生成 SQL
    │
    ▼
┌──────────────────┐     不通过      ┌─────────────────┐
│ SQL 安全校验      │ ──────────────▶ │ 返回安全告警     │
│ (AST 解析)        │                 │ "不允许执行该操作"│
└────────┬─────────┘                 └─────────────────┘
         │ 通过
         ▼
┌──────────────────┐
│ 强制约束注入      │
│ - 只读事务        │
│ - LIMIT 10000    │
│ - 查询超时 30s    │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐     报错        ┌─────────────────┐
│ 执行 SQL          │ ──────────────▶ │ 重试 (最多3次)   │
└────────┬─────────┘                 └─────────────────┘
         │ 成功
         ▼
┌──────────────────┐
│ 返回结果 + 记录审计│
└──────────────────┘
```

---

## 6. 多轮对话设计

### 6.1 MVP 阶段策略：上下文透传，LLM 自主判断

**决策：MVP 阶段支持多轮对话，但采用最简实现方案。**

- **前端**：维护当前会话的对话历史（`conversationHistory`），每次请求将最近 N 轮（建议 N=5）对话一并发送给后端。
- **Java 网关**：透传 `conversationHistory` 给 Python AI 服务。
- **Python AI 服务**：将 `conversationHistory` 注入到 LLM 的 Prompt 中，由 LLM 自主判断是否需要引用上文。

**示例：**

```
用户第 1 轮：上个月订单总额多少？
AI 第 1 轮：上月订单总额为 1,234,567 元。[使用了 order_info.total_amount]

用户第 2 轮：按部门拆分看看
（系统将第 1 轮对话 + 第 2 轮问题一起发给 LLM）
（LLM 判断需要引用上文的"上月订单总额"，生成按部门分组的 SQL）

用户第 3 轮：再加一个退款率
（LLM 理解需要在上一个查询基础上增加退款率字段，自动关联 refund_record 表）
```

### 6.2 上下文窗口裁剪策略

当对话轮数增多，Token 可能超出限制，采用以下裁剪策略（优先级从高到低）：

1. **保留最近 3 轮完整对话**（用户问题 + AI 回答摘要 + 使用的 SQL）
2. **更早的对话只保留摘要**（由上一轮 AI 回答的"口径说明"字段充当摘要）
3. **当前问题的 Schema Context 始终保留**（不能裁剪召回的表结构）

### 6.3 会话管理

| 项目 | 说明 |
|------|------|
| **会话创建** | 用户打开问答页面自动创建，生成 `conversationId` |
| **会话持久化** | 存储在数据库的 `conversation` 表中，支持用户查看历史会话 |
| **会话过期** | 超过 24 小时无交互自动过期 |
| **切换数据源** | 用户切换数据源时，自动创建新会话（避免跨库上下文污染） |

---

## 7. 字段可信度量化算法

### 7.1 可信度分数模型

采用 **0-100 数值评分**，而非简单的高/中/低等级。系统内部用数值计算，对外展示时映射为等级：

| 分值区间 | 等级 | 含义 |
|----------|------|------|
| 80-100 | 🟢 高可信 | 经过人工确认或大量成功验证 |
| 50-79 | 🟡 中可信 | 在 skills.md 中定义，或有少量成功验证 |
| 0-49 | 🔴 低可信 | 仅有 Schema 注释，未经验证 |

### 7.2 初始分数设定

| 来源 | 初始分 |
|------|--------|
| 仅有数据库 Schema 注释 | 30 |
| 在 `skills.md` 中被定义 | 60 |
| 被数据分析师手动确认/打 Tag | 90 |
| 被管理员直接设为"高可信" | 100 |

### 7.3 动态调整规则

| 事件 | 分数变化 | 说明 |
|------|----------|------|
| 该字段在一次成功查询中被使用，且用户点了"赞" | **+10**（上限 100） | 正向反馈 |
| 该字段在一次成功查询中被使用，用户未反馈 | **+2**（上限 100） | 弱正向（默认成功即有价值） |
| 用户点了"踩"，原因选"数据不准" | **不直接扣分，进入审核队列** | 防恶意/误操作 |
| 管理员审核确认"踩"有效 | **-15**（下限 0） | 确认降级 |
| 管理员审核驳回"踩" | **不变** | 防止误伤 |
| 累计同一字段被 3 个不同用户"踩"且无人审核 | **-5**（下限 0）+ **触发管理员告警** | 群体反馈的自动降级 |

### 7.4 可信度对 SQL 生成的影响

在 Prompt 中注入可信度规则：

```
你在生成 SQL 时，必须遵循以下字段优先级：
- 高可信字段（80-100 分）：优先使用，这些字段的含义已得到验证。
- 中可信字段（50-79 分）：可以使用，但请结合字段注释和 skills.md 判断。
- 低可信字段（0-49 分）：谨慎使用，如果存在同义的高可信字段，必须使用高可信字段。
- 废弃字段（Tag 为"废弃"）：绝对禁止使用。

本次查询涉及的字段可信度：pay_amount(95), total_amount(60), old_amount(20,已废弃)
```

### 7.5 防恶意反馈机制

- **单用户限频**：同一用户对同一字段每天最多触发 1 次"踩"。
- **审核队列**：普通用户的"踩"不直接生效，进入管理员审核队列。
- **管理员特权**：管理员/分析师的"踩"直接生效，权重为普通用户的 3 倍。
- **群体自动降级**：同一字段被 3 个不同用户"踩"，系统自动将可信度 -5 并告警。

---

## 8. Prompt 管理策略

### 8.1 Prompt 模板化管理

系统中涉及的所有 Prompt **禁止硬编码在代码中**，统一存储在数据库 `prompt_template` 表：

| 字段 | 说明 |
|------|------|
| `id` | 模板 ID |
| `name` | 模板名称（如 "sql_generation"、"chart_generation"、"intent_recognition"） |
| `template` | Prompt 模板内容，支持 `{{变量}}` 占位符 |
| `version` | 版本号，每次修改自动递增 |
| `is_active` | 是否启用（同一 name 可以有多个版本，但只有一个 is_active） |
| `created_at` | 创建时间 |
| `updated_by` | 最后修改人 |

### 8.2 系统中的 Prompt 模板清单

| 模板名 | 用途 | 注入变量 |
|--------|------|----------|
| `schema_retrieval_query` | 将用户问题改写为检索查询 | `question`, `conversationHistory` |
| `sql_generation` | 生成 SQL | `question`, `schema`, `skills_md`, `field_confidence`, `few_shot_templates`, `conversationHistory`, `error_message`(重试时) |
| `chart_generation` | 生成 Echarts 配置 | `question`, `sql`, `data_preview`, `column_types` |
| `intent_recognition` | 意图识别 | `question`, `conversationHistory` |

### 8.3 Token 预算控制

每个 Prompt 模板有 Token 预算上限（默认 4000 Token），各部分按优先级分配：

| 优先级 | 内容 | 预算 |
|--------|------|------|
| 1（必须保留） | 用户问题 + 召回的 Schema | 1500 Token |
| 2（尽量保留） | skills.md 关键段落 | 1000 Token |
| 3（空间允许才加） | Few-shot SQL 模板 | 800 Token |
| 4（空间允许才加） | 历史对话上下文 | 500 Token |
| 5（剩余空间） | 可信度详情 | 200 Token |

如果总 Token 超出预算，从优先级最低的部分开始裁剪。

### 8.4 Prompt 版本管理

- 每次修改 Prompt 模板，自动创建新版本（version +1），旧版本保留不删除。
- 管理员可以在后台查看所有历史版本、对比差异、回滚到旧版本。
- 修改 Prompt 后，系统提示"该 Prompt 已修改，建议进行一轮测试查询验证效果"。

---

## 9. Skills.md 版本管理与向量化同步

### 9.1 版本管理

| 项目 | 说明 |
|------|------|
| **存储** | skills.md 内容存储在数据库 `skills_document` 表中，而非文件系统 |
| **版本控制** | 每次保存自动生成新版本（version +1），保留历史版本，支持回滚 |
| **编辑锁** | 同一时间只允许一人编辑（乐观锁：保存时检查版本号，冲突则提示刷新） |
| **编辑角色** | 仅"数据分析师"和"管理员"角色可编辑 skills.md |

### 9.2 向量化同步触发机制

```
skills.md 被修改并保存
    │
    ▼
系统自动创建一条"向量化任务"记录（状态：PENDING）
    │
    ▼
定时任务（每 5 分钟）扫描 PENDING 任务
    │
    ▼
异步执行向量化：
  1. 解析 skills.md 的各段落
  2. 对每个段落生成 Embedding
  3. 更新向量数据库中的对应记录
    │
    ▼
任务状态更新为 COMPLETED / FAILED
    │
    ├── 成功：下次查询自动使用新版本
    └── 失败：告警管理员 + 查询仍使用旧版本（不中断服务）
```

### 9.3 向量化期间的查询策略

- **向量化进行中**：查询使用旧版本的向量数据，不中断服务。
- **向量化失败**：告警管理员，查询继续使用旧版本，不降级。
- **首次向量化（冷启动）**：系统启动时检查，若向量库为空则自动触发全量向量化。

---

## 10. 数据源连接与安全规范

### 10.1 数据源连接配置

| 项目 | 说明 |
|------|------|
| **连接信息** | JDBC URL、账号、密码，存储在 `datasource_config` 表中 |
| **密码加密** | 密码使用 AES-256 加密存储，密钥通过环境变量或配置中心注入，**禁止明文存储** |
| **强制只读账号** | 系统连接业务数据库**必须使用只读账号**（仅有 SELECT 权限），后台页面在配置数据源时进行提示 |
| **连通性测试** | 配置数据源后，系统自动执行 `SELECT 1` 测试连通性，失败则阻止保存 |
| **连接池配置** | 每个数据源独立连接池，AI 服务端最大 10 连接，Java 端最大 5 连接 |

### 10.2 多数据源路由

- 用户在前端选择数据源 → 前端传 `datasourceId` → Java 从数据库读取连接信息 → 传给 Python AI 服务。
- Python AI 服务每次请求动态创建/复用连接池（以 `datasourceId` 为 key 缓存连接池）。
- 数据源禁用时：强制断开该数据源的所有连接池。

---

## 11. 查询历史与价值沉淀

### 11.1 审计日志（基础能力）

每条查询记录以下信息：

| 字段 | 说明 |
|------|------|
| `user_id` | 查询用户 |
| `datasource_id` | 查询的数据源 |
| `question` | 用户的原始问题 |
| `generated_sql` | AI 生成的 SQL |
| `execution_time_ms` | SQL 执行耗时 |
| `row_count` | 返回行数 |
| `is_success` | 是否成功 |
| `error_message` | 失败时的错误信息 |
| `used_tables` | 使用的表列表 |
| `used_fields` | 使用的字段列表 |
| `user_feedback` | 用户反馈（赞/踩/null） |
| `created_at` | 查询时间 |

### 11.2 高频查询模板沉淀

- 管理员可以在审计日志中，将某条"赞"较多的成功查询**提升为模板**。
- 模板包含：用户问题的改写版本 + 对应的 SQL + 使用的表。
- 模板在 RAG 召回阶段作为"模板召回"的数据源（见 4.2 节）。
- 模板可以按数据源分类，支持启用/禁用。

### 11.3 慢查询预警

- SQL 执行超过 5 秒的查询，自动标记为"慢查询"。
- 后台提供慢查询列表，支持按数据源、用户、时间筛选。
- 管理员可以分析慢查询并优化 skills.md（如增加"该表数据量大，请加时间范围过滤"的提示）。

---

## 12. Echarts 图表生成策略

### 12.1 图表类型决策规则

LLM 根据数据特征决定图表类型，规则写入 `chart_generation` Prompt 模板：

| 数据特征 | 推荐图表 | 说明 |
|----------|----------|------|
| 有时间维度 + 数值指标 | 折线图 | 展示趋势变化 |
| 分类维度 + 数值指标（≤10 类） | 柱状图 | 展示对比 |
| 分类维度 + 占比 | 饼图/环形图 | 展示构成比例 |
| 多个数值指标 | 表格 | 不适合用图表展示 |
| 2 个数值指标的相关性 | 散点图 | 展示分布与关联 |
| 地理维度 | 地图 | 展示地域分布 |

### 12.2 前端图表交互

- **自动展示**：LLM 推荐的图表类型默认展示。
- **手动切换**：用户可以一键切换图表类型（柱状图 ↔ 折线图 ↔ 饼图等），前端根据数据格式自动适配。
- **兜底机制**：如果 LLM 返回的 Echarts Option 无效（JSON 解析失败或必填字段缺失），前端降级为纯表格展示，不报错。
- **导出**：支持导出图表为 PNG 图片。

### 12.3 图表生成的数据预处理

LLM 生成图表配置时，不能把全量数据塞进去（可能几千行），需要预处理：

- **数值聚合**：如果数据超过 50 行，前端或后端先做聚合（如按分类求和/求平均），再传给 LLM 生成图表配置。
- **数据预览**：传给 LLM 的是数据的前 20 行 + 列类型信息 + 总行数，而非全量数据。
- **LLM 只生成 Option 配置**：实际的数据绑定由前端根据 SQL 返回的列名自动完成。

---

## 13. Schema 同步与变更感知

### 13.1 同步策略

| 项目 | 说明 |
|------|------|
| **全量同步** | 管理员手动触发 或 定时任务（每天凌晨 2 点） |
| **同步内容** | 表名、字段名、字段类型、字段注释、索引信息 |
| **同步方式** | 通过 JDBC 的 `DatabaseMetaData` API 读取 Schema |
| **向量库更新** | Schema 变更后，自动触发向量化重建（增量更新：仅更新变更的表） |

### 13.2 变更检测与告警

同步完成后，系统对比新旧 Schema，检测以下变更：

| 变更类型 | 告警级别 | 处理方式 |
|----------|----------|----------|
| 新增表 | 低 | 提示管理员配置该表的 Tag 和是否启用 |
| 删除表 | **高** | 检查该表是否在 skills.md 中被引用、是否有历史查询使用，是则告警 |
| 新增字段 | 低 | 提示管理员配置 Tag |
| 删除字段 | **高** | 同上，检查依赖关系 |
| 字段类型变更 | **中** | 提示管理员确认是否影响已有查询 |
| 字段注释变更 | 低 | 自动更新向量库 |

### 13.3 血缘图谱与变更级联

- 系统解析历史成功的 SQL，构建表级/字段级的依赖关系图（DAG）。
- 当检测到某张表被删除时，自动在血缘图中找到所有依赖该表的 skills.md 条目和高频查询模板，批量告警。

---

## 14. 多环境支持

| 环境 | 说明 |
|------|------|
| **开发环境** | 使用本地数据库 + 本地向量库 + 大模型 API 的 test key |
| **测试环境** | 使用测试数据库 + 测试向量库 + 大模型 API 的 test key |
| **生产环境** | 使用生产只读数据库 + 生产向量库 + 大模型 API 的正式 key |

- 三个环境的 skills.md **各自独立**（生产环境的 skills.md 由分析师维护，开发/测试用简化版）。
- 大模型 API Key 通过环境变量注入，不存储在代码或数据库中。
- 通过 Spring Boot 的 `profile` 机制区分环境配置。

---

## 15. 成本控制与监控

### 15.1 Token 用量监控

| 监控项 | 说明 |
|--------|------|
| 每次查询消耗的 Token 数 | 记录到审计日志 |
| 每日/每月总 Token 用量 | 按数据源、用户汇总 |
| 大模型 API 调用次数 | 按日统计 |
| 预估费用 | 根据各模型单价计算 |

### 15.2 成本控制措施

| 措施 | 说明 |
|------|------|
| **用户每日查询配额** | 默认每用户每天 100 次查询（可配置） |
| **Token 预算上限** | 单次查询消耗不超过 8000 Token（含 Prompt + Completion） |
| **费用告警** | 每日 API 费用超过阈值时告警管理员 |
| **模型降级** | 简单查询（意图识别判定为简单）使用更便宜的模型，复杂查询使用高能力模型 |

---

## 16. 错误处理与降级策略

### 16.1 各环节的错误处理

| 环节 | 可能的错误 | 处理方式 |
|------|-----------|----------|
| **向量检索** | 向量数据库不可用 | 降级为仅使用 skills.md 中的核心表（取前 5 张），记录告警 |
| **LLM 调用** | API 超时/限流 | 重试 1 次，仍失败则返回"AI 服务繁忙，请稍后再试" |
| **LLM 返回** | 返回非法 JSON / SQL 格式错误 | 提取失败信息，走重试流程（最多 3 次） |
| **SQL 执行** | 数据库连接失败 | 返回"数据源连接异常，请联系管理员" |
| **SQL 执行** | SQL 语法错误 | 走重试流程（携带错误信息让 LLM 修复） |
| **SQL 执行** | 查询超时 | 返回"查询超时，请缩小查询范围或增加筛选条件" |
| **用户取消** | 用户在等待中关闭页面 | 后端检测到连接断开后，取消正在执行的 SQL（通过 JDBC Statement.cancel()） |

### 16.2 Java-Python 通信的降级

- Java 调 Python 设置 **60 秒超时**。
- Python 服务不可用时，Java 返回"AI 服务暂时不可用，请稍后再试"，并告警运维。
- Python 服务的健康检查：Java 每 30 秒调用 Python 的 `/health` 端点，连续 3 次失败则标记为不可用。

---

## 17. 数据脱敏与审计合规

### 17.1 敏感字段脱敏

- 管理员可以在后台为字段打上"敏感"Tag（如身份证号、手机号、工资）。
- 查询结果返回时，系统自动对"敏感"字段进行脱敏处理：
  - 身份证号：`3101**********1234`
  - 手机号：`138****5678`
  - 金额类（如工资）：是否脱敏由管理员配置
- 脱敏规则在 **Java 网关层**处理（返回给前端之前），而非在 Python AI 服务中。

### 17.2 审计合规

- 所有查询日志保留 **180 天**（可配置）。
- 支持按用户、时间范围导出审计日志。
- 敏感字段的查询记录额外标记，便于安全审计。

---

## 18. 冷启动与初始化流程

### 18.1 系统首次上线流程

```
管理员首次登录系统
    │
    ▼
步骤 1：配置数据源（填写 JDBC URL、测试连通性）
    │
    ▼
步骤 2：一键拉取 Schema（系统自动读取所有表和字段的元信息）
    │
    ▼
步骤 3：配置 skills.md（可选，初期可以先写核心表的说明）
    │
    ▼
步骤 4：点击"初始化向量库"（系统自动将 Schema + skills.md 向量化）
    │
    ▼
步骤 5：配置用户权限（哪些用户可以访问哪些数据源）
    │
    ▼
完成！用户可以开始查询
```

### 18.2 可信度冷启动

- 不需要干等用户反馈。系统初始化时，自动将 skills.md 中明确提到的字段预设为"中可信（60 分）"。
- 如果企业已有 BI 报表，可以导入报表底层 SQL，系统自动解析高频字段，预设为"高可信（80 分）"。

---

## 19. `skills.md` 模板示例（针对单库）

```md
# 数据库：trade_db (交易域)

## 核心表说明
- order_info：订单主表（事实表），包含所有主订单信息。
- order_item：订单明细表，商品粒度的金额和折扣在这里。
- refund_record：退款记录表。

## 关键关联规则 (Join Path)
- 订单与明细：`order_info.order_id = order_item.order_id`
- 订单与退款：`order_info.order_id = refund_record.order_id`

## 关键口径与字段防坑
- **GMV**：取 `order_info.total_amount`，且 `status IN (2,3,4)`（支付成功及之后的阶段）。千万不要用 `create_amount`。
- **退款金额**：取 `refund_record.actual_refund`，且 `refund_status = 'SUCCESS'`。
- **废弃字段**：`order_info.old_user_id` 已废弃，一律使用 `user_id`。

## 复杂查询模板 (Few-shot)
### 排名查询（窗口函数）
问题示例："每个部门业绩排名前三的员工"
```sql
SELECT department_id, employee_name, sales_amount, ranking
FROM (
    SELECT department_id, employee_name, sales_amount,
           ROW_NUMBER() OVER(PARTITION BY department_id ORDER BY sales_amount DESC) as ranking
    FROM employee_sales
    WHERE sales_date >= DATE_SUB(CURDATE(), INTERVAL 1 MONTH)
) t
WHERE ranking <= 3
```

### 连续N天查询
问题示例："连续3天有退款的订单"
```sql
SELECT order_id, refund_date, consecutive_days
FROM (
    SELECT order_id, refund_date,
           COUNT(*) OVER(PARTITION BY order_id ORDER BY refund_date
                         RANGE BETWEEN INTERVAL 2 DAY PRECEDING AND CURRENT ROW) as consecutive_days
    FROM refund_record
    WHERE refund_status = 'SUCCESS'
) t
WHERE consecutive_days >= 3
```


---

## 20. 系统核心功能点清单（MVP 落地版）

### 20.1 前端：员工问答端 (C端)

- **数据源切换器**：顶部下拉框，用于选择当前要查询的业务域/数据库（如"订单域"、"财务域"）。切换时自动创建新会话。
- **自然语言对话框**：支持文本输入、历史提问记录快速点击。
- **多轮对话**：支持上下文追问（如"按部门拆分"、"加一个退款率列"），展示完整对话历史。
- **查询过程白盒展示**：在出结果前，流式展示 AI 的思考过程（例如："正在召回订单表..."、"已生成查询 SQL..."），提升用户信任感。
- **多模态结果展示**：
  - 数据表格（支持分页、排序、导出 Excel/CSV）。
  - 自动推荐的 Echarts 图表（柱状图、折线图、饼图等，支持一键切换图表类型）。
  - 图表导出为 PNG。
- **可解释性与溯源面板**：结果旁附带"口径说明"，展示本次查询用到了哪些表、哪些高可信字段，依据了哪条 `skills.md` 规则。
- **用户反馈机制**：每条结果下方提供"赞/踩"按钮，并支持点击"踩"后选择原因（如：数据不准、图表类型错、口径不对）。
- **会话管理**：左侧展示历史会话列表，支持切换、删除会话。

### 20.2 后台：治理与管理端 (B端)

- **数据源与 Schema 管理**：
  - 数据库连接配置（JDBC URL、只读账号、加密密码）。
  - 连通性测试。
  - Schema 自动一键拉取与定时同步（每天凌晨 2 点）。
  - 表与字段的启用/禁用控制（屏蔽系统表和敏感表）。
  - Schema 变更检测与告警。
- **`skills.md` 业务知识库管理**：
  - 在线 Markdown 编辑器，支持按业务域编写表关系、业务口径、核心指标定义。
  - 版本管理：查看历史版本、对比差异、回滚。
  - 知识库的向量化状态查看与手动触发重新向量化。
- **Prompt 模板管理**：
  - 查看和编辑各环节的 Prompt 模板。
  - 版本管理与历史回滚。
  - Token 预算配置。
- **字段 Tag 与可信度工作台**：
  - 字段打标面板：为字段批量添加业务标签（如"金额"、"时间"、"用户ID"、"敏感"）。
  - 可信度手动调整：数据分析师可直接将某字段设为"高可信"。
  - **用户反馈处理中心**：集中查看 C 端用户的"踩"，分析师确认后决定是否对某字段进行降级。
- **查询审计与 SQL 实验室**：
  - 查询日志列表：记录谁、在什么时间、问了什么问题、AI 生成了什么 SQL、是否报错。
  - 慢查询分析：针对执行超过 5 秒的 SQL 进行记录与预警。
  - 高频查询模板管理：将优质查询提升为模板。
- **安全与权限控制**：
  - 用户角色管理（普通员工、分析师、管理员）。
  - 库级别的数据访问授权。
- **成本监控看板**：
  - Token 用量统计（按日/月、按用户、按数据源）。
  - 费用预估与告警阈值配置。

### 20.3 后台：单库数据治理与血缘引擎 (治理端核心)

在单库几百张表的环境下，若不进行治理，NL2SQL 系统将成为"垃圾进、垃圾出"的重灾区。因此必须提供专门的治理与血缘模块：

- **自动化单库血缘图谱**：
  - **动态血缘解析**：系统后台定时解析大模型生成的成功 SQL（以及历史高频查询日志），自动逆向生成一张表与表、字段与字段之间的有向无环图（DAG），展示"经常被联合查询的表簇"。
  - **变更级联预警**：当 DBA 修改了某张表的结构（如删除了某个旧状态字段），血缘引擎会立即报警，提示管理员"该字段被高频查询，且在 `skills.md` 中被引用，可能导致后续 AI 生成报错"。
- **冗余字段与废弃表隔离（库内治理）**：
  - **废弃字段雷达**：扫描库中常年未被查询命中、或者被分析师打上"弃用" Tag 的字段，生成待清理报表。
  - **白名单/黑名单机制**：直接将某些"测试表"、"历史归档表"放入召回黑名单，彻底避免大模型检索到这些表而产生幻觉。
- **单库口径一致性校验看板**：
  - 如果一个业务指标（如 GMV）既能从 `order_info` 算出来，也能从 `payment_record` 算出来，系统会提示存在"口径分歧风险"，并建议在 `skills.md` 中强制指定唯一权威口径。

---

## 21. 完整的项目开发路径（Roadmap）

为了确保项目稳步落地，建议将整个开发周期分为以下四个阶段。每一步都明确前后端的开发任务和交付物：

### 阶段一：核心链路贯通（MVP 验证期）

**目标**：不搞复杂的系统管理，先证明"大模型 + skills.md"能在一个单库中稳定生成复杂 SQL 并查出正确数据。

- **后端架构搭建**：
  - **Java (Spring Boot)**：搭建项目骨架，实现用户鉴权、数据源管理 CRUD、前端接口代理。
  - **Python (FastAPI + LangGraph)**：搭建 AI 服务，定义 State、Nodes 和自我修复循环。
  - **服务通信**：Java 通过内部 HTTP（RestTemplate）调用 Python AI 服务。
- **核心任务**：
  - 在 Python 端用 LangGraph 编写一个极简的有向图：`生成SQL → 校验并执行 → 如果报错带着错误信息回去重写`。
  - SQL 安全拦截：基于 `sqlglot` 实现基本的 AST 校验（仅允许 SELECT）。
  - SQL 执行沙箱：强制只读事务 + LIMIT 10000 + 超时 30 秒。
  - 硬编码读取本地的 `skills.md` 文件和数据库 Schema（不做向量检索）。
- **前端任务**：
  - 开发极简的"对话问答框"和"数据表格展示"页面。
  - 支持数据源下拉切换。
- **阶段复用说明**：
  - Java 的鉴权、数据源管理、前端代理层在后续阶段**直接复用**。
  - Python 的 LangGraph 有向图结构**直接复用**，后续阶段仅增加/修改节点。
  - SQL 安全拦截器**直接复用**，后续阶段增加更多校验规则。
- **阶段产出**：一个能通过自然语言查出单库数据、能自我纠错、具备基本安全防护的 AI 服务。

### 阶段二：引入 Schema RAG 与动态配置（进阶拓展期）

**目标**：解决单库表太多导致大模型幻觉的问题，引入向量数据库，并将静态配置转为动态系统管理。

- **后端任务**：
  - 接入向量数据库（如 Milvus / Chroma）。
  - 开发 Schema 自动拉取与向量化脚本（将几百张表的注释、`skills.md` 存入向量库）。
  - **核心逻辑**：在 LangGraph 中增加 `Schema_Retriever_Node`，实现"自然语言提问 → 向量检索召回 Top 5 相关表 → 拼装 Prompt → 生成 SQL"的 RAG 完整链路。
  - 开发后台管理接口：支持在页面上配置数据库连接、在线编辑 `skills.md`（含版本管理）。
  - Prompt 模板从硬编码迁移到数据库存储。
- **前端任务**：
  - 开发后台管理页面（数据源管理、知识库在线编辑、Prompt 模板管理）。
  - 前台增加"查询过程白盒展示"（让用户看到正在召回哪几张表）。
- **阶段复用说明**：
  - 阶段一的 LangGraph 有向图**直接复用**，仅增加 `Schema_Retriever_Node`。
  - 阶段一的 SQL 安全拦截器和沙箱约束**直接复用**。
- **阶段产出**：能够支撑单库 200+ 张表检索的 NL2SQL 系统，且具备后台配置能力。

### 阶段三：可信度闭环与图表可视化（用户体验期）

**目标**：解决"AI 胡说八道"和"展示干瘪"的问题，建立字段 Tag、可信度体系以及丰富的图表。

- **后端任务**：
  - 设计并创建数据表，用于存储"字段 Tag"、"字段可信度评分"和"用户反馈记录"。
  - 修改 Prompt 逻辑，在生成 SQL 时强行注入"优先使用高可信字段"的规则。
  - 增加 `Data_Visualizer_Node`（LLM 生成 Echarts Option）。
  - 实现多轮对话的上下文透传和裁剪策略。
- **前端任务**：
  - 对话框结果区增加自动生成的 Echarts 图表展示（支持手动切换图表类型）。
  - 结果区下方增加"赞/踩"反馈按钮及原因弹窗。
  - 开发后台"字段 Tag 打标工作台"和"用户反馈审核中心"页面。
  - 实现多轮对话的前端交互（历史消息展示、上下文追问）。
- **阶段产出**：带有图表展示、可人工纠错、可信度动态升降、支持多轮对话的完整闭环问答系统。

### 阶段四：单库治理与血缘沉淀（企业级深化期）

**目标**：解决脏数据表污染和长效运维问题，构建单库血缘与隔离机制，这也是本项目的核心护城河。

- **后端任务**：
  - 开发定时任务：解析历史成功的 SQL 记录（AST 抽象语法树解析），自动构建单库内表与字段的有向无环图（血缘 DAG）。
  - 开发黑白名单拦截网关：在 RAG 召回前，强制过滤掉被打上"废弃"标签的脏表。
  - 增加非 SELECT 语句（如 DROP/DELETE）的安全拦截器（已有的基础版扩展）。
  - 实现 Schema 变更检测与血缘级联告警。
  - 实现高频查询模板沉淀机制。
  - 实现成本监控与用量配额。
- **前端任务**：
  - 开发后台"单库血缘关系可视化图谱"页面（使用 Echarts 关系图或 G6 等图库）。
  - 开发"废弃字段/隔离表"的管理列表。
  - 开发成本监控看板。
  - 审计日志增加"提升为模板"操作。
- **阶段产出**：具备自动血缘追踪、废弃表隔离、高安全性拦截、成本可控的企业级智能查数平台。

---

## 22. 汇报专用：逻辑可行性推演与核心技术难点

### 22.1 Schema RAG 的"一子错，满盘皆输"问题

- **逻辑漏洞**：用户问"华东区退款率"，如果 RAG 阶段没有召回 `refund_record`（退款表），而是召回了 `order_info`（订单表），大模型生成的 SQL 必然是错的。在 RAG 链路中，**召回错了，生成一定错**。
- **解决方案**：采用 Hybrid Search（混合检索）策略（见第 4 节），融合语义检索、关键词检索和模板检索三路召回，并在阶段二逐步引入。

### 22.2 可信度体系的"冷启动"与"恶向反馈"风险

- **逻辑漏洞**：项目初期，所有字段都是低可信度，AI 容易乱用字段导致体验差，用户不用，就更没有反馈，陷入死循环。同时，如果某个用户出于误解，不断给正确的数据点"踩"，会导致高可信字段被错误降级。
- **解决方案**：
  1. **冷启动突破**：skills.md 中定义的字段自动设为"中可信"，已有 BI 报表的高频字段预设为"高可信"（见第 18 节）。
  2. **防恶意反馈**：采用审核队列 + 群体反馈阈值机制，普通用户的"踩"不直接生效（见第 7.5 节）。

### 22.3 极其复杂的业务 SQL（嵌套与聚合）生成稳定性

- **逻辑漏洞**：大白话往往隐藏着极度复杂的嵌套逻辑。例如："把昨天每个部门业绩排名前三的员工拉出来"。这在 SQL 中需要用到 `ROW_NUMBER() OVER(PARTITION BY)` 窗口函数。如果只是把 Schema 给大模型，它经常会用简单的 `GROUP BY` 和 `LIMIT`，导致逻辑完全错误。
- **解决方案**：`skills.md` 中必须包含 Few-shot 的高级 SQL 模板（见第 19 节模板示例）。当意图识别到"排名前几"、"连续三天"等字眼时，动态注入这些模板指导大模型写出复杂 SQL。

### 22.4 动态 SQL 注入与数据越界查询风险

- **逻辑漏洞**：员工可能会说"删除订单表里今天的记录"或者"给我看总裁办的工资明细"。大模型如果真的生成了 `DELETE` 或绕过权限的查询，后果不堪设想。
- **解决方案**：SQL 安全拦截网关 + 执行沙箱（见第 5 节），从 AST 解析、只读账号、查询超时、敏感字段脱敏等多层防护。

---

## 23. 关键设计决策汇总（ADR）

以下为本文档中做出的重要架构决策，便于开发时快速查阅：

| 决策项 | 决策 | 理由 |
|--------|------|------|
| 前端调用方式 | 统一调 Java，Java 代理调 Python | 统一入口、统一鉴权、前端简单 |
| Java-Python 通信协议 | 内部 HTTP（RestTemplate/Feign） | 简单可靠，内网延迟可忽略 |
| Python 服务状态 | 无状态 | 每次请求由 Java 传入上下文，便于水平扩展 |
| SQL AST 解析工具 | sqlglot (Python) | 支持多数据库方言，与 Python AI 服务同层 |
| 向量数据库 | 阶段一不需要，阶段二选型 | MVP 先用硬编码，降低复杂度 |
| MVP 多轮对话 | 支持，采用上下文透传方案 | 体验不会太差，实现简单 |
| 可信度模型 | 0-100 数值评分 | 比等级更精细，便于动态调整 |
| 反馈降级机制 | 审核队列，非直接生效 | 防恶意/误操作 |
| SQL 执行沙箱 | 只读账号 + LIMIT + 超时 | 多层防护，任何一层都能兜底 |
| Prompt 管理 | 数据库存储 + 版本管理 | 可在线修改，可回滚 |
| Skills.md 存储 | 数据库而非文件系统 | 便于版本管理、权限控制、多环境同步 |
| 密码存储 | AES-256 加密 | 安全合规 |
